# Web App Workspace

This package contains the LinguaChat web client built with React, TypeScript, and Vite.

Useful commands from the repository root:

```bash
npm --prefix apps/web install
npm run dev:web
npm run build:web
```

The long-term cross-platform plan is to keep browser-specific UI here and move shared contracts into `packages/shared`.
